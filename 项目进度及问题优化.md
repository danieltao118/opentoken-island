# 项目进度及问题优化

> OpenToken Island（Windows）——生财 token 排行榜 + 多供应商额度面板。
> 分支 `codex/reliable-local-stats`，已装应用 `C:\Users\ty\AppData\Local\OpenToken Island\`（v0.1.5，热部署 server.js）。

## 当前状态（2026-09-01）

**全部核心功能正常**：上传排行榜（2h 自动循环 + 当天定向）、v1/v2 双通道、五供应商额度面板（glm / cursor / kimi / codex = ok；grok 待用户重登）。GUI 代理常驻（GUI 退出不掉）、版本接管、卸载清理均已验证。

## 修复时间线

| 日期 | 内容 | 提交 |
|---|---|---|
| 08-16 | 上传 600s 超时根因修复（44GB 扫描 ~24min 必超时）：当天定向上传 + 30min 可配超时 + 2h 自动上报 + 停全量 preview 空转 | 32c8e5d |
| 08-16 | v2 信封放行 + `/api/shutdown`；0.1.5 代理 detached 常驻 + 版本接管 + NSIS 卸载清理 | 59cae98 / dac57ea |
| 08-17 | v2 六层穿透（schema 类型/tz 空值/事件类型/Unix 秒会话/字节级转发/unhoured 数组），上游首次 accepted | 581f20e → 227b6d7 |
| 08-18~21 | （Codex agent）额度面板 Grok/Cursor/GLM、按日期 junction 扫描家目录、daemon 自愈、同步状态机 | 431d799 |
| 09-01 | （仓库外）`~/.codex/config.toml` 加 `prefer_websockets=false`：修 codex-cli 0.144 WS 426 对接 opencodex 网关 | — |
| 09-01 | 被墙端点代理兜底：requestText 三级链（直连→DNS 兜底→CONNECT 隧道，`OPENTOKEN_UPSTREAM_PROXY` 默认 7892）修 codex 额度直连 chatgpt.com 失败 | 9070ff0 |
| 09-01 | Cursor 额度卡大字与 Grok/Codex/Kimi 统一为「剩余 XX%」（已用退到明细行；apiPercentUsed 数据语义不变） | e8003e2 |
| 09-09 | 排行榜核验：v1 日用量全部 200/accepted、自动补 8/19 缺口、dated 扫描生效；v2 新拦截（register 信封字段 + inventory/plan_snapshot 事件）已放行，accepted {hourly,raw} | 5bc50bf |
| 09-23 | 官方重接：connect 新链接（换令牌）+ service install（30 分钟任务 PT30M Ready）+ self-update 0.3.22→0.3.30；代理补 GET /tokenrank/api/* 透传修 client-config 404；榜单重绑 35859，rank 39/10.35 亿 matched | 本次 |

## 已知问题 / 注意事项

1. **NSIS 静默安装（/S）在本机稳定卡死**——升级用构建产物手动拷贝（exe + server.js + html），或交互式安装。
2. **grok 额度依赖 CLI token**：x.ai OIDC 6h 过期、仅 grok 进程运行时续期；过期后重登 `grok login --device-auth` 或开桌面 app。
3. 代理隧道兜底只在直连失败后触发，国内端点（zhipu/kimi）不受影响；Clash 未运行时 chatgpt.com 额度会回到不可用（如实降级）。
4. `windowsHide: true` 必须保留在 server.js 全部 execFile 调用点（否则每分钟闪黑框）。
5. 已归档 Codex 历史日志在 `F:\backup\codex-archive-before-202608\`（8GB，>30 天未活跃）；再次归档必须按文件 mtime 边界，不得按会话开始日期。

## 验证口径

- 测试：`npm test`（13 套契约测试）+ `cargo test --manifest-path src-tauri/Cargo.toml`（16 项）
- 部署后健康：`curl 127.0.0.1:4174/api/health`（appVersion=0.1.5）+ `/api/summary` 的 quotaFeeds 全量状态
